<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    
    <h1>Welcome to quoting dojo.</h1>

    <h3>Add a new quote</h3>
    <form action="addquote" method="post">
        <label for="name">Name: </label>
        <input type="text" name="name">
        <label for="quote">Quote: </label>
        <textarea name="quote" id="quote" cols="30" rows="10"></textarea>
        <button id="add" name="button" value="add quote">Add my quote!</button>
    </form>
    <form action="quoteboard">
    <button id="skip" name="button" value="skip to quote dashboard">Skip to quotes</button>
    </form>

    <% if(typeof(errors) != 'undefined' ) { %>
        <% for (var x in errors) { %>
         <h3><%= errors[x].message %></h3>
        <% } %>
    <% } %>
</body>
</html>